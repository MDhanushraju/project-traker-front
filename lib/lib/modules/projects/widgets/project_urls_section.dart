import 'package:flutter/material.dart';

/// Section showing project URLs. Uses theme.
class ProjectUrlsSection extends StatelessWidget {
  const ProjectUrlsSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('URLs'));
  }
}
