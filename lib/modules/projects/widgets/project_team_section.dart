import 'package:flutter/material.dart';

/// Section showing project team. Uses theme.
class ProjectTeamSection extends StatelessWidget {
  const ProjectTeamSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const ListTile(title: Text('Team'));
  }
}
